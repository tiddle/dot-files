{
  home-manager = {
    enable = true;
  };
  zoxide = {
    enable = true;
    enableZshIntegration = true;
  };
  git = {
    enable = true;
    userName = "Carlo Cruz";
    userEmail = "me@carlocruz.com";
  };
  htop = {
    enable = true;
  };
  thefuck = {
    enable = true;
  };

  nerdfonts = {
    enable = true;
  };

  wget = {
    enable = true;
  };

  curl = {
    enable = true;
  };

  ripgrep = {
    enable = true;
  };

  neofetch = {
    enable = true;
  };

}